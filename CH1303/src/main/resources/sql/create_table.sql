CREATE TABLE item2 (
	item_id INTEGER(5) AUTO_INCREMENT,
	item_name VARCHAR(20),
	price INTEGER(6),
	description VARCHAR(50),
	picture_url VARCHAR(200),
	picture_url2 VARCHAR(200),
	PRIMARY KEY (item_id)
);
